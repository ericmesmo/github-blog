import styled from "styled-components";

export const ContentContainer = styled.main`
    width: 100%;
    max-width: 1120px;

    margin: 0 auto;
    padding: 0 1.5rem;

    .content {
        padding: 2.5rem 2rem;
    }
`